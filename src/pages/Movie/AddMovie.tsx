import MovieForm from "../../components/Movie/MovieForm";
import PageLayout from "../PageLayout";

const AddMovie = () => {
  return (
    <PageLayout pageTitle="Add New Movie">
      <MovieForm />
    </PageLayout>
  );
};

export default AddMovie;
    